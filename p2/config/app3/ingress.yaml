apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-three
  namespace: part-2
spec:
  rules:
  - host: "app3.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-three
            port:
              number: 80